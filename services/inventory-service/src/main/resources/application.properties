# Fixed at 8080 internally -- this is the port INSIDE the container.
# docker-compose.yml maps this to different host ports per service, and
# the orchestrator (Phase 2+) talks to it over the Docker network by
# container name, not by host port at all.
server.port=8080
spring.application.name=inventory-service
